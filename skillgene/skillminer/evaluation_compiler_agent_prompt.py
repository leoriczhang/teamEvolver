EVALUATION_COMPILER_AGENT_PROMPT = """你现在必须严格使用 `evaluation-compiler-agent-skill` 这个 skill，处理我指定的、针对**同一个主题簇**产出的全部语义分析报告，并编译出**两个配套产物**：一个 skill + 这个 skill 对应的评测任务。

【输入目录】
{INPUT_DIR}
{REFLECTION_CONTEXT}
【任务定位】
1. 这不是重新做语义发现，而是把已有语义分析报告编译为可执行产物。
2. 输入的语义报告可能来自"同一个主题簇被切成多个批次"后，各批次分别产出的报告；
   因此不同报告里可能出现同一个可复用单元的多次描述，你必须把它们识别为同一个并合并去重。
3. 你的最终产物只有两个，且必须同步产出：
   - 产物一：一个可执行的 skill 定义（SKILL.md），多个能力维度作为其中的并列章节
   - 产物二：这个 skill 对应的评测任务（EVALUATION.md），评测维度与 skill 能力维度一一对应
4. skill 讲"该怎么做"，评测任务讲"怎么判断做得对不对"，二者一体两面、最后必须互相对照检查。

【执行要求】
1. 你必须严格按 `evaluation-compiler-agent-skill` skill 执行。
2. 只产出"一个 skill + 一个评测任务"，不允许产出多个平级 skill。
3. 不允许退回旧版的五类评分协议（judge / pairwise / hard_negative / reward / retrieval）。
4. 多个能力维度必须是同一个 SKILL.md 内的并列章节，而不是多个独立文件/多个 skill。
5. 每个维度必须写清可执行逻辑（触发条件 / 判断依据 / 处理步骤 / 输出形态 / 边界与例外 / 来源），不能只有维度名。
6. 必须处理不确定性、失败模式、适用范围和边界，不要过早简化、不要抹平冲突。
7. 每条规则、每个考核样例都必须可追溯到语义报告证据；证据不足处必须显式标注"证据不足/待补充"，禁止臆造。

【跨批次合并去重——必须执行】
1. 按语义报告中的"规范名 + 功能签名"判断两份报告里的单元是不是同一个。
2. 是同一个 -> 合并成一个维度，综合各报告的证据、边界、例外。
3. 合并时若不同批次对同一单元有冲突描述，不要抹平，记入该维度的"边界与例外"。
4. 禁止让同一条规则因来自不同批次而重复成多个并列维度。
5. 维度内聚自检：对每一对相邻维度问"它们靠什么区分？"，写不出区分理由的必须合并。

【置信档——必须为 skill 判档】
1. 汇总所有语义报告末尾的"结构化缺口清单"，统计高/中/低严重度缺口的数量与分布。
2. 依据下表为 skill 判定置信档，并在 SKILL.md 中写明判档依据：
   - 生产级：证据充分、多批次互证、矛盾已消解、覆盖完整、无高严重度未解决缺口
   - 候选级：主题清晰、证据中等，但仍有中等缺口或少量未消解分歧
   - 草稿级：证据薄/多来自单一来源/存在高严重度未解决缺口/大量待补维度
3. 高严重度缺口越多、越集中在核心维度，置信档越低。
4. 把"待补维度/未解决缺口"显式列在 SKILL.md 中，作为后续证据补充时的升级坑位表。
5. 证据不足时不允许硬判为生产级。

【评测任务（EVALUATION.md）要求】
1. 考核目标：检验"运用该 skill 做出的判断/处理"是否正确，并回归检验 skill 是否覆盖完整。
2. 必须写清被考核对象的输入/输出格式。
3. 评测维度必须与 skill 的能力维度一一对应（每个能力维度都要有对应考核维度）。
4. 每个评测维度写清：评分标准、通过条件、常见错误。
5. 必须给出正例与负例的评判示范；考核样例必须追溯到语义报告中的真实案例，禁止臆造。
6. 必须包含"与 SKILL.md 的维度对应表"。
7. 边界与例外要有对应考核，呼应 skill 的边界/例外章节。

【对照检查——关键收尾，必须执行】
产出 SKILL.md 与 EVALUATION.md 后，你必须把两者摆在一起逐条核对并修正：
- 覆盖一致：skill 每个能力维度在评测里都有对应考核维度；评测里没有 skill 中不存在的凭空维度。
- 口径一致：同一维度，skill 的"判断依据"与评测的"评分标准"说的是同一件事、不矛盾。
- 边界一致：skill 的边界/例外，评测里也有相应考核。
- 可追溯一致：两边引用的来源指向同一批语义报告证据。
发现的不一致就地修正；无法修正的（证据冲突/不足）显式记入两个文件的"待补/存疑"处。

【输出目录】
{OUTPUT_DIR}

【输出要求】
请直接开始执行，在 `{OUTPUT_DIR}/<skill-name>/` 下同时产出：
- SKILL.md（必需）
- EVALUATION.md（必需）
（references / assets / common 仅在真正需要时才建）

如果某处证据不足，你必须明确指出不足，而不是省略或编造。
最后请明确报告：置信档判定结论及依据、对照检查的结论与已修正/仍存疑的不一致项。
"""

INPUT_DIR = "semantic_reports/"
OUTPUT_DIR = "compiled_skill/"
