SEMANTIC_DISCOVERY_AGENT_PROMPT = """你现在必须严格使用 `semantic-discovery-agent-skill` 这个 skill，处理我指定的单个样本包，并结合全局 package_notes 生成详细语义分析报告。

【当前样本包】
{INPUT_DIR}
{REFLECTION_CONTEXT}
【全局 package_notes】
{NOTES_DIR}/global_notes/ 与 {NOTES_DIR}/package_notes/
（注意：notes 位于样本包集合的顶层目录，不在当前样本包内部）

【任务定位】
你面对的是一批**主题文档**（可以是任意领域：规章制度、操作流程 SOP、行业知识库、审批规范等）。
当前样本包是同一个主题簇被切出来的**其中一个批次**（也可能整簇就是一个批次）。
你的目标是从当前样本包中挖掘出**可复用的决策单元 / 判断结构 / 流程逻辑**，
为后续的"skill 编译 Agent"把这批文档凝练成一个可执行 skill 提供依据。
你不假设任何特定业务，一切归纳都必须能追溯到样本包中的具体文档证据。

由于同一个主题簇可能被切成多个批次，各批次会分别产出语义报告，后续 skill 编译 Agent 需要把
**多份报告里的同一个单元/结构认出来并合并**。因此你必须配合两件事（见下方【统一命名约定】和
【结构化缺口清单】），降低跨批次合并的难度、并把本批次发现的信息缺口显式沉淀下来。

【执行要求】
1. 你必须严格按 `semantic-discovery-agent-skill` skill 执行。
2. 当前样本包内容是第一证据来源。
3. 当前样本包对应的 note 是第二证据来源。
4. 其他 package_notes 只能作为弱参考，不能压制本包中的真实张力。
5. 你必须先提出候选可复用单元，再反驳不合适结构，再保留值得继续验证的结构。
6. 不允许直接输出最终标签、唯一 schema 或成品 skill。
7. 必须输出详细 md 分析报告，而不是短摘要。

【关于 common/ 子文件夹的特殊处理】
当前样本包内部可能存在一个 `common/` 子文件夹（如术语总则、通用规范基准等），它是**所有样本包共享的完整基础资产**，每个样本包中的 common/ 内容都是完全一致的。你在分析时必须遵循以下规则：

1. **common/ 的定位：背景规则基准 / 跨包不变量**
   - common/ 中的内容（例如术语总则、通用规范基准）是所有样本包共享的背景资产
   - 它应被视作"背景规则基准"，用于帮助你理解当前样本包中具体证据的含义
   - 它**不是当前样本包特有的证据**，不应被当作当前样本包独有的语义观察

2. **不允许让 common/ 压制本包的真实张力**
   - 不要因为 common/ 中写了某条规则，就把当前样本包中的观察硬往 common/ 的框架上套
   - 当本包具体证据与 common/ 存在张力时，这本身就是有价值的发现，必须保留并明确指出
   - 不要用 common/ 的通用结论去压制本包中具体案例暴露出的边界/例外/冲突

3. **不允许把 common/ 内容混入"本包反复出现的判断单元"**
   - "本包反复出现"是指基于本包非 common/ 部分的证据
   - common/ 在所有包中都存在，不能作为当前包"反复出现"的证据来源
   - 否则所有样本包都会因为共享 common/ 而产出高度相似的语义报告，失去差异化价值

4. **common/ 的正确用法**
   - 帮助理解术语、通用规范、命名约定等背景信息
   - 为本包具体证据提供参照坐标
   - 指出本包证据在 common/ 规则框架下是符合、偏离、还是突破
   - 作为后续验证建议的参考依据

5. **差异化语义必须来自 common/ 之外的内容**
   - 候选可复用单元、候选判断/流程结构、反复出现的判断单元，都必须主要基于当前样本包中非 common/ 部分的证据归纳
   - 如果某结构仅靠 common/ 才能成立，请明确标注"证据仅来自共享 common/，不算本包独有发现"

【特别强调的检查项】
- 不能把显式措辞直接等同于潜在语义
- 必须指出被反驳或暂不保留的结构
- 必须指出信息缺口
- 必须给出后续验证建议
- 不能因为 package note 写了什么，就直接把它当作已被内容完全证明的事实
- 不能把 common/ 中的共享规则当作当前样本包特有的语义观察
- 不能引入文档中不存在的领域知识或臆造规则
- 最终报告必须在"证据来源"部分明确区分：本包独有证据 / 当前包 note / 共享 common/ / 其他包弱参照

【统一命名约定——为跨批次合并铺路（重要）】
同一个主题簇可能被切成多个批次，每个批次各出一份语义报告，后续 skill 编译 Agent 需要把不同报告里
"其实是同一个东西"的单元合并起来。为降低它的对齐难度，你在命名候选可复用单元 / 候选判断流程结构时，必须遵守：

1. 用"稳定、可跨批次复用"的规范名，而不是随口起的、只在本批次有意义的名字
   - 命名应基于该单元的"功能/职责"，而非某个具体案例的表面词
   - 例：用"退款金额阈值校验"这类功能名，而不是"案例3里那个判断"
2. 命名尽量与 common/ 中已确立的术语、总则用词保持一致（若 common/ 有对应术语，直接沿用其措辞）
3. 每个候选单元/结构，除"规范名"外，可附一个简短的"功能签名"一句话描述它"在什么输入下、依据什么、产出什么"，
   便于编译 Agent 判断两个批次里的单元是不是同一个
4. 如果你怀疑本批次某个单元与其他批次可能重复，请在该单元下显式写出"可能与之合并的同类单元线索"，
   但不要臆断其他批次的内容——只写"值得编译 Agent 核对是否为同一单元"

【结构化缺口清单——把信息缺口沉淀成可被消费的结构（重要）】
不要把"信息缺口"只写成一段散文。你必须在报告末尾额外产出一个**结构化的缺口清单**，
让后续流程能直接消费（用于判断证据是否充分、是否需要跨批次继续验证、以及编译时的置信判档）。
每条缺口至少包含：
- 缺口ID（本报告内唯一，如 GAP-01）
- 缺口描述（缺了什么信息 / 哪个判断无法稳定成立）
- 影响到的候选单元/结构（引用上面的规范名）
- 严重度（高 / 中 / 低——高=缺了它对应能力维度无法成立；低=细节待补）
- 是否需要跨批次验证（是 / 否）
- 可能的补全来源线索（若能判断，缺口的答案可能在簇内哪类文档里）
这个清单既是"skill 里待补维度的坑位表"，也是编译 Agent 判置信档的重要依据。

【输出目录】
{OUTPUT_DIR}

【输出要求】
请直接开始执行，并输出符合 skill 契约的一份完整 md 报告。
报告除常规内容外，必须包含：（1）候选单元/结构均使用统一规范名并附功能签名；（2）末尾的结构化缺口清单。
如果你发现某些地方证据不足，必须明确写出，而不是强行下结论。
"""

INPUT_DIR = "sample_packages/"
OUTPUT_DIR = "semantic_reports/"
