SAMPLE_PACKAGE_CONSTRUCTOR_AGENT_PROMPT = """你现在必须严格使用 sample-package-constructor-agent-skill 这个 skill，处理我指定的目录，不能偷懒，不能跳步，不能只给概括性结果。

【你在流水线中的位置】
你是"簇内分批器"。你拿到的输入是**一个主题簇**——一批已被上游判定围绕同一主题的文档。
你不负责重新做主题划分，也不做跨主题聚类。
你要解决的核心矛盾是"上下文容量"：一个主题簇可能太大，一次喂给后续语义发现 Agent 装不下，你要把它切成"装得下"的批次样本包。

【目标目录（一个主题簇）】
{INPUT_DIR}
{VALIDATION_FEEDBACK}
【执行要求】
1. 你必须严格按照 sample-package-constructor-agent-skill skill 文档执行。
2. 这不是普通聊天总结任务，而是一次严格的 skill 执行任务。
3. 你必须遵守 skill 中的全部硬约束、执行流程、覆盖规则、自检要求和输出结构。
4. 不允许为了省事弱化任何硬约束。
5. 不允许只做"看起来差不多"的结果，必须做真正符合 skill 的结果。
6. 不允许被 skill 中的示例锚定。示例里的 样本包001/002/003 只是命名示例，不代表固定只生成 3 个包。
7. 批次数量必须根据簇的实际规模（文档篇数、内容总量、下游一次能消化多少）动态决定，而不是机械输出固定数量。
8. 不允许把不同批次机械分工成"常规包 / 冲突包 / 失败包 / 边界包 / 伪相似包"。
9. 每个批次内部都应尽量同时包含以下领域无关的关键张力：
   - 常规/稳定情形
   - 冲突情形
   - 失败/异常情形
   - 例外/边界情形
   - 表面相似但实质不同的情形
10. 批次之间的区别，来自"把一个大簇拆成可消化的片"，而不是"不同子主题"或"不同张力专柜"。
11. 必须生成符合 skill 要求的真实产物，而不是只输出文字描述。

【体量开关——最重要的前置判断】
在做任何切分之前，你必须先判断这个簇的体量：
- 小簇（整簇内容可被后续语义发现 Agent 一次性消化；默认必须满足非 common 总内容 <= 100000 字符，有效文档 <= 8 篇仅作辅助判断）
  -> 跳过切分，把整个簇作为"单一批次样本包"直接透传。此时允许单文件完整进入该唯一包；仍必须复制 common/、仍必须写 notes。
- 大簇（整簇一次喂不进下游上下文）
  -> 按 skill 规则切成若干批次样本包。
核心原则：能一次看完就别切；一次看不完才切。不要对本可一次消化的小簇强行切碎。

【来源类型识别规则】
输入的主题簇中，内容分为三类来源，你必须按以下规则识别和处理：

0. Common 共享来源（Common Source）—— 最高优先级，必须原封不动复制：
   - 簇内名为 `common/` 的子文件夹
   - 该文件夹内的所有文件与子目录结构，必须原封不动地完整复制到每一个批次样本包中
   - 禁止对 common/ 中的任何文件做裁剪、删减、摘录、改写
   - 每个批次样本包中都必须存在一个与 `common/` 结构完全一致的 `common/` 文件夹，内容逐字节一致
   - common/ 是跨批次共享的基础资产（如术语总则、通用规范基准、共享参照资料等）
   - common/ 中的文件不参与"单文件反整份复制"的裁剪约束
   - common/ 中的文件不计入批次的"单文件来源裁剪密度"自检与覆盖密度统计

1. 单文件来源（File Source）：
   - 簇根目录下直接存在的文件（不是文件夹，且不在 common/ 内）
   - 处理方式：大簇切分时，对每个批次围绕要覆盖的内容裁剪相关子集，禁止整份复制原文件；满足容量上限的小簇单包透传除外
   - 同一个单文件来源在不同批次中应有不同裁剪，不能近乎原样重复搬运
   - 若某个单文件来源接近全量保留，你必须明确说明为什么这个文件几乎全部内容都相关

2. 目录来源（Directory Source）：
   - 簇内存在的子文件夹/目录（除了 common/ 之外）
   - 处理方式：对每个批次，从目录中采样多个文件，不能只象征性抽取两三篇
   - 每个目录来源在每个批次中都必须达到足够采样密度
   - 跨全部批次的并集，目录来源中的大部分文件都应被覆盖到

重要：你必须先识别是否存在 `common/` 子文件夹。如果存在，它优先级最高，必须原封不动完整复制；其他内容再按"文件 / 文件夹"判断为单文件来源或目录来源。

【特别强调的检查项】
零、体量判断（最优先）
- 是否先做了体量判断，决定"跳过切分（小簇）"还是"执行切分（大簇）"
- 小簇是否被正确透传为单一批次而非被无谓切碎

一、Common 共享来源处理
- 如果簇内存在 `common/`，它必须被原封不动完整复制到每一个批次中，逐字节一致
- 禁止对 common/ 中的任何内容做裁剪、摘录、改写

二、来源覆盖（大簇切分时）
- 每个批次都必须覆盖所有单文件来源
- 每个批次都必须覆盖所有目录来源，且目录来源不能在任何批次中完全缺席

三、目录来源采样密度（大簇切分时）
- 对目录来源不能只象征性抽取两三篇文件
- 每个目录来源在每个批次中都必须达到足够采样密度
- 跨全部批次的并集，目录来源中的大部分文件都应被覆盖到

四、单文件来源裁剪（大簇切分时）
- 大簇切分时，单文件来源不能通过整份复制原文件来完成覆盖；小簇单包透传除外
- 单文件来源在每个批次中都必须是做过选择后的子集
- 不允许同一个单文件来源在多个批次中近乎原样重复搬运

五、批次组织
- 批次之间的区别来自"把大簇拆成可消化的片"，不是"不同子主题"，更不是"不同张力专柜"
- 不能把批次命名或理解为"常规包/失败包/冲突包"

六、输出结构
- 必须生成符合 skill 要求的样本包目录
- 必须生成 package_notes
- 必须生成 global_notes
- 不允许只输出口头说明代替真正结果

【切片深度与去重量化指标——产物会被程序自动校验，不达标将被打回重做】
你的产物会被一个自动校验器逐项检查。以下指标是硬性口径，不满足会被判为不合格：
1. 深度优先：宁可少包深切，不要多包浅切。证据不够厚时，优先加深每个包对来源文件的切片深度，而不是增加包数。
2. 每个样本包中非 common 的实质内容不少于 8000 字符（若整簇本身更小，则整簇透传，天然满足）。
3. 每个样本包内容总量不超过 100000 字符（下游一次可消化的上限）。
4. 跨全部样本包的并集，吸收的原文内容不少于簇总量的 25%。远低于该值说明每包都切得太浅（例如每个文件只切两三百字的"目录式摘要"）。
5. 同一个单文件来源在不同样本包中的片段，互相重复度必须低于 85%：不同包必须切原文的不同部分（不同章节/不同段落），不是同一段内容换个说法；逐字节相同直接判不合格。
   推荐做法：先把每个原文按章节/主题划成互不重叠的区段，再把不同区段分配给不同包；每包分到的区段应完整、连续、足量。
6. 大簇切分时，单文件来源片段量达到原文 90% 及以上视为整份复制，判不合格（原文不足 2000 字符的短文件除外）。总量 <= 100000 字符且只生成一个包的小簇透传不适用此限制，但必须在 package_notes 记录体量判断。

【执行方式】
请按下面顺序执行，不要跳步：
1. 先识别簇内是否存在 `common/` 文件夹。如果存在，规划把它原封不动完整复制到每个批次
2. 再识别簇内的单文件来源与目录来源（根据文件/文件夹类型判断，排除 common/）
3. 做体量判断（体量开关）：小簇 -> 跳过切分整簇透传；大簇 -> 继续切分
4. 大簇情形：对候选内容切片并打粗粒度价值标签（常规/冲突/失败/例外/伪相似/覆盖）
5. 大簇情形：先满足来源覆盖与目录采样密度
6. 大簇情形：用尽量少的、下游装得下的批次覆盖整个簇，决定批次数量
7. 组装样本包：确定每个批次横截面 -> 选入片段 -> 按来源文件回填到镜像目录
8. 把 common/ 原封不动复制进每一个批次
9. 输出 package_notes 与 global_notes
10. 最后做自检，并明确指出是否仍有未满足项（包括体量判断与 common/ 复制完整性）

【输出目录】
{OUTPUT_DIR}

【输出要求】
请直接开始执行。
先简要说明你会如何严格按照该 skill 处理这个主题簇（包括体量判断结论），然后立即产出符合 skill 的最终结果。
如果某一项要求最终没有满足，你必须明确列出未满足项，而不能假装已经完成。

补充硬约束：
- 必须先做体量判断；小簇跳过切分整簇透传，大簇才切分
- 如果存在 `common/` 文件夹，必须把它原封不动完整复制到每一个批次中，内容逐字节一致
- 禁止对 common/ 中的任何文件做裁剪、删减、改写
- 不允许默认只生成固定数量的批次
- 不允许把不同批次机械分成常规/冲突/失败/边界/伪相似五种包
- 每个批次内部都应尽量同时包含这五类关键张力
- 大簇切分时：每个批次都必须覆盖所有单文件来源与目录来源
- 目录来源不能只象征性抽两三篇文件
- 大簇切分时，单文件来源不能整份复制原文件来完成覆盖（common/ 与满足容量上限的小簇单包透传除外）
- 每包非 common 实质内容 >= 8000 字符，总量 <= 100000 字符（默认口径）
- 跨包并集吸收率 >= 簇原文总量的 25%
- 同一单文件来源在不同包中的片段互相重复度 < 85%，必须切原文不同部分
- 不要引入原始文档中不存在的内容
- 若最终结果没有满足这些要求，请明确列出未满足项
"""

INPUT_DIR = "data/input/"
OUTPUT_DIR = "sample_packages/"
