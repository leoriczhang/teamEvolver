# teamEvolve 生产可用需求 — 交付验收对照

> 对照文档：《teamEvolve生产可用需求》
> 核对代码库：`/home/zhangpengkun/定制项目/teamevolver-prod-0915`
> 核对日期：2026-09-18
> 方式：逐项对照代码实现（只读核对，未修改功能代码）

## 总览

共提炼 11 项需求：**9 项已完成（Done）**，**2 项部分完成（Todo 收尾）**。

| # | 需求 | 状态 |
|---|------|------|
| 1 | 四智能体数据解析脚本 + Rollout 接口 | ⚠️ 部分完成 |
| 2 | 租户级 + 阶段级独立大模型配置（火山 Ark，多模型测试） | ✅ 已完成 |
| 3 | BadCase 定位到 Trace，输出问题类型/问题描述 | ✅ 已完成 |
| 4 | Trace 分析结果经钩子上传 Doris | ⚠️ 部分完成 |
| 5 | 候选"已审阅/已采纳"勾选 + 操作记录 | ✅ 已完成 |
| 6 | 数据集导出 + 管理 + 跑批 | ✅ 已完成 |
| 7 | 经验总结落经验库 + 错误次数计数 | ✅ 已完成 |
| 8 | SKILL 存储支持 OV + 本地 NAS | ✅ 已完成 |
| 9 | 数据源定时器 + 拉取入队列 | ✅ 已完成 |
| 10 | SKILL 导入/导出统一接口（ZIP/市场/Git） | ✅ 已完成 |
| 11 | 实验界面类 VSCode + 在线调试 SKILL | ✅ 已完成 |

---

## ✅ Done（已完成）

### 需求 2：租户级 + 阶段级独立大模型配置
- **租户级**：`teamEvolver/tenants/registry.py`（`config_overrides` 覆盖全局，`llm_*` 不在 `SERVICE_WIDE_CONFIG_KEYS` 强制全局清单内，允许按租户覆盖）；`teamEvolver/proxy/routes.py:2784-2856`（`POST /api/model-settings` 写入租户 config 并刷新 EnginePool）；前端 `web-ui/src/views/ModelSettingsView.tsx`。
- **阶段级**：`team_skills/evolution/prompt_studio.py`（`_STAGE_CATALOG` 定义 7 个进化阶段，`set_stage_settings` / `stage_call_options` 支持每阶段独立 provider/base_url/model/api_key/temperature/max_tokens）；调用点如 `team_skills/evolution/stages/trace_analyze.py:134-140`。
- **火山 Ark + 多模型**：`teamEvolver/config_store/defaults.py:37-44`、`config/config*.yaml` 指向 Ark 网关；`teamEvolver/llm.py` 为 OpenAI-wire 兼容客户端，改 `model` 字段即可接多模型测试。

### 需求 3：BadCase 定位到 Trace + 问题类型/描述
- `team_skills/evolution/stages/trace_analyze.py`：一 Trace 一 turn、带唯一 `trace_id`；受控 `PROBLEM_TYPE_LABELS`（10 类，含中文 label）+ `problem_description`；`_parse_llm_analysis` 强制校验；贯穿 ingest 主链路（`session_ingestion/service.py:79-92`）。测试 `tests/test_trace_analyze.py`。

### 需求 5：候选"已审阅/已采纳/已驳回"标记 + 操作记录
- 后端 `team_skills/candidates/feedback.py`（持久化 reviewed/adopted/rejected，`update()` 追加带 actor/时间戳的审计 history，乐观锁防并发，并兼容旧版双标记记录）。
- 路由 `teamEvolver/proxy/routes.py`（`PATCH /api/skill-candidates/{job_id}/feedback` 仅允许人工更新 reviewed；发布成功自动写 adopted；驳回操作自动写 rejected 并保留候选历史）。
- 前端 `web-ui/src/views/dashboard/CandidateFeedback.tsx`（"已审阅/已采纳/已驳回"三个 checkbox + "操作记录（N）"折叠区，其中仅"已审阅"可手动操作）。

### 需求 6：数据集导出 + 管理 + 跑批
- 导出 `team_replay/datasets/collections.py:287-306`（`export_zip`）+ `teamEvolver/proxy/dataset_routes.py:215-233`。
- 管理 `DatasetCollectionStore`（create/update/change_item/delete）+ 全套 `/api/datasets` 路由。
- 跑批 `team_replay/datasets/batch.py`（`DatasetBatchRunner`，线程池 + 信号量，支持取消/重启恢复）+ `/api/datasets/{id}/runs` 路由；另有 `team_replay/lab/service.py` 实验跑批。
- 前端 `web-ui/src/views/DatasetsView.tsx`。

### 需求 7：经验总结落库 + 错误次数计数
- `team_skills/evolution/runtime/experience_library.py`：每条经验为一段清晰文本 `description`（≤1200 字），`kind ∈ {defect, exemplary}`；`occurrence_count` 按 session 去重累加即"这个错误犯了几次"。
- 落库触发 `team_skills/evolution/runtime/orchestrator.py:300-311`；来源自评审阶段 `stages/analyze.py` + `stages/judge.py` 输出的 `skill_experiences`。
- 读取 `GET /api/skill-experiences`（`teamEvolver/proxy/routes.py:4906-4943`）；前端 `web-ui/src/views/ExperienceLibraryView.tsx` 展示"累计 N 次"及 `defect_occurrences` 统计。
- **口径说明**：需求"得知这个错误犯了几次"= 次数，`occurrence_count` 已满足；不需要新增字段。（若未来要"按具体 Agent 分别计数"是另一维度，当前未实现，但非本需求要求。）

### 需求 8：SKILL 存储支持 OV + 本地 NAS
- 存储抽象层 `teamEvolver/storage/{base,__init__,local,viking}.py`：统一 key 契约，`normalize_backend` 归一 local/viking/postgres；`LocalObjectStore` 明确支持本地磁盘及挂载 NAS；`build_object_store` 工厂 + viking 不可用回退 local。
- 配置 `teamEvolver/config.py:94-142`（`sharing_skill_backend` = viking/local，`sharing_skill_local_root` 可指向 NAS 挂载点）；`config/config.yaml:32-49`。

### 需求 9：数据源定时器 + 拉取入队列
- `session_ingestion/pull/scheduler.py`：`DEFAULT_SCHEDULE`（enabled/time HH:MM/timezone/window=previous_day/max_sessions）；`previous_day_window` 实现"每天 0 点拉昨天全天"；`DatasourcePullRuntime` 调度循环 + 去重防重复；拉取结果经 ingest → `session_store.save_queued` 进队列等待分析。
- 路由 `session_ingestion/pull/routes.py:153-209`（`GET/PUT /api/datasource/schedule`、`POST .../run`）。测试 `tests/test_datasource_scheduler.py`。

### 需求 10：SKILL 导入/导出统一接口（ZIP/市场/Git）
- 统一接口 `team_skills/transfer/packages.py:41-44`（`TransferAdapter` 协议：`read` 导入 / `write` 导出）。
- `team_skills/transfer/service.py:31-45` 注册三 adapter：`zip` / `marketplace` / `git`。
- 三渠道实现 `team_skills/transfer/channels.py`（ZIP 解包/打包、市场拉取/上传）+ `team_skills/transfer/git.py`（`new_branch` 新建分支 / `new_repository` 新建仓库上传）。
- 前端 `web-ui/src/views/skills/SkillTransferModal.tsx`（三渠道下拉）。

### 需求 11：实验界面类 VSCode + 在线调试 SKILL
- 工作台 `web-ui/src/views/SkillWorkbenchView.tsx`：文件树资源管理器、多标签编辑器、快捷键（Ctrl+S/Ctrl+P/Ctrl+B/F5）、底部调试控制台、右侧 True Replay 运行调试。
- 编辑器 `web-ui/src/views/workbench/CodeEditor.tsx`（CodeMirror，多语言高亮）。
- 后端 `teamEvolver/proxy/skill_workspace.py`（Workspace = 代码文件夹：`GET /api/replay-lab/workspace`、读/写文件带 sha256 乐观锁）；在线调试 `web-ui/src/views/SkillLabView.tsx` → `POST /api/replay-lab/runs` → `team_replay/lab/service.py`。

---

## ⚠️ Todo（部分完成，需收尾/确认）

### 需求 1：四智能体数据解析脚本 + Rollout 接口 —— 适配器已完成，Rollout 待确认
- ✅ **已完成**：四个智能体适配器均存在且解析逻辑完整（标题、用户/工号、trace_id、时间戳、心跳过滤）：
  - 产品智能体 `session_ingestion/adapters/product-agent.py`
  - 客户智能体 `session_ingestion/adapters/客户智能体-upclaw.py`（另有 `客户智能体.py`）
  - 陆网规划智能体 `session_ingestion/adapters/陆网规划智能体-upclaw.py`
  - 网点智能体 `session_ingestion/adapters/网点智能体.py`（走 `_shared/sf_agent_adapter.py` + `_shared/session_meta.py`）
- ⚠️ **待办**：Rollout 目前是**通用 replay 框架 + 绑定机制**（`team_replay/engine.py`、`teamEvolver/proxy/replay_routes.py`、`scripts/import_skillopt.py:165-185`）。适配器头部注释中的 `agent_host`/`rollout_concurrency` 未被运行时读取，**未见四个智能体逐一装配/跑通的专属 Rollout 通路**。
  - **收尾项**：为四个智能体分别配置/绑定 replay adapter（`agent_host` 等），并留存端到端 Rollout 跑通的实测记录。
  - **清理项**：`陆网规划智能体-upclaw.py` 存在残留 `.orig` 文件，建议清理。

### 需求 4：Trace 分析结果经钩子上传 Doris —— 钩子机制已完成，Doris 上传器未内置
- ✅ **已完成**：钩子机制完整——`team_skills/evolution/stages/trace_analyze.py` 提供 `register_trace_analysis_hook` / 环境变量 `TEAMEVOLVER_TRACE_ANALYSIS_HOOK` / `dispatch_trace_result`（fail-open）；`analyze_and_dispatch_traces` 对每条 trace 结果触发一次钩子，携带 `problem_type` / `problem_description` / `trace_id` / `session_id` 等；已接入 ingest 主链路（`session_ingestion/service.py:90-92`）。测试 `tests/test_trace_analyze.py`。
- ⚠️ **待办**：真正写入 Doris 的上传器为**设计上刻意留空的可插拔外挂**，代码库未随附具体实现。
  - **收尾项**：实现并注册 Doris 上传钩子（通过环境变量或 `register_trace_analysis_hook`），并验证端到端实际写入 Doris；留存注册配置与实测记录。

---

## 备注
- 需求 8/10 中的"对象存储"由 OpenViking（`viking` 后端）承载，代码未使用字面 `tos`/`s3` SDK 命名。若验收严格要求 TOS/S3 SDK 直连，需另行确认 `OpenVikingObjectStore` 底层对接。
- 除上述 2 项外，其余需求均前后端闭环（存储 + API + 前端展示），并有对应测试覆盖。
